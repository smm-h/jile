package jile.cs;

/**
 * Unlike an {@link Algorithm} whose complexities are studied, a
 * {@link Heuristic} must always have the least possible complexities. In other
 * words, it is meant to be "practical rather than precise". Optimality is not a
 * goal for a heuristic; in other words even though it may not yield "the best"
 * results or solutions, it does yield "good enough" ones. It is often
 * estimative. Heuristics are used most often in {@link ai}.
 */
public interface Heuristic {

}
